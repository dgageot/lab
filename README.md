# Download the world

+ Install Maven
+ Install Java 8
+ Install Node
+ Run these commands from this directory

```bash
mvn clean install
mvn dependency:go-offline
npm install -g bower
npm install
bower install
webdriver-manager update
```
